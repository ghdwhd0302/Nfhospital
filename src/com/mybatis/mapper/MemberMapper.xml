<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
   <!-- 회원가입 -->
   <insert id="join" parameterType="memberInfoVO">
      insert into member_info
      (id, password, name, residentNum, email, address, phoneNum, addPhoneNum, birth, gender)
      values(#{id}, #{password}, #{name}, #{residentNum}, #{email}, #{address}, #{phoneNum}, #{addPhoneNum}, #{birth}, #{gender})
   </insert>
   
   <!-- 아이디 중복검사 -->
   <select id="checkId" parameterType="string" resultType="int">
      select count(id) from member_info where id = #{memberId}
   </select>
   
   <!-- 로그인 -->
   <select id="login" parameterType="map" resultType="string">
   		select name from member_info
   		where id = #{id} and password = #{password}
   </select>
   
</mapper>