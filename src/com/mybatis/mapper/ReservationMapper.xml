<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Reservation">
   <!-- 예약하기 -->
   <insert id="add" parameterType="reservationVO">
      insert into reservation
      (id, name, phoneNum, medicalDept, doctor, scheduleDate, medicalDate)
      values(#{id}, #{name}, #{phoneNum}, #{medicalDept}, #{doctor}, #{scheduleDate}, #{medicalDate})
   </insert>
   
   <!-- 예약조회 -->
   <select id="view" parameterType="reservationVO">
      select name, phoneNum, medicalDept, doctor, scheduleDate, medicalDate
      from reservation 
      where id = #{id}
   </select>
   
   <!-- 예약삭제 -->
   <delete id="del" parameterType="reservationVO">
   	delete from reservation
   	where id = #{id} 
   </delete>
</mapper>