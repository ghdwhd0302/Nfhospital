<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Reservation">
   <!-- 예약하기 -->
   <insert id="reseervation" parameterType="reservationVO">
      insert into reservation
      (id, name, phoneNum, medicalDept, doctor, scheduleDate)
      values(#{id}, #{name}, #{phoneNum}, #{medicalDept}, #{doctor}, #{scheduleDate})
   </insert>
   
   <!-- 예약조회 -->
   <select id="reservations" parameterType="reservationVO">
      select name, phoneNum, medicalDept, doctor, scheduleDate, medicalDate
      from reservation 
      where id = #{id}
   </select>
   
   <!-- 예약삭제 -->
   <delete id="reservationDelete" parameterType="reservationVO">
	   	delete from reservation
	   	where id = #{id} 
   </delete>
   
   <!-- 예약수정 -->
   <update id="reservationUpdate" parameterType="reservationVO">
   		update tbl_reservation
      	set medicalDept = #{medicalDept}, doctor = #{doctor},  scheduleDate = #{scheduleDate}}
      	where id = #{id}
   </update>
</mapper>