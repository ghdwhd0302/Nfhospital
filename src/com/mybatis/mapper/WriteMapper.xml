<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Write">

	<!-- 감사합니다 게시글 전체 목록 -->
	<select id="ThankyouWriteList" resultType="writeVO">		
		select write_num, title, content, id, time
		from thank_you_write order by write_num desc						
	</select>
	
	<!-- 감사합니다 게시글 선택 출력 -->
	<select id="hankyouWriteChoose" resultType="writeVO">		
		select title, content, id, time
		from thank_you_write		
	</select>
	
	<!-- 건의합니다 게시글 선택 출력 -->
	<select id="suggestWriteChoose" resultType="writeVO">		
		select title, content, id, time
		from client_sound_write 			
	</select>
		
	<!-- 내가 작성한 글 목록 -->
	<select id="myWriteList" resultType="writeVO">		
		select write_num, title, content, id, time
		from tbl_write order by write_num desc						
	</select>
	
	<!-- 내가 작성한 글 선택 출력 -->
	<select id="myWriteChoose" resultType="writeVO">		
		select title, content, id, time
		from tbl_write 			
	</select>
	
   <!-- 글 작성 -->
   <insert id="write" parameterType="writeVO">
      insert into tbl_write
      (title, content, id, time)
      values(#{title}, #{content}, #{id}, now())
   </insert>
   
   <!-- 글 수정 -->
   <update id="writeUpdate" parameterType="writeVO">
      update tbl_write
      set title = #{title}, content = #{content}, time = now()}
      where id = #{id}
   </update>
   
   <!-- 글 삭제 -->
   <delete id="writeDelete" parameterType="writeVO">
      delete from tbl_write
      where where id = #{id}
   </delete>
   
</mapper>